set ideadMatching icon locked
    - after filled up user home page from user, change the icon to unlocked one.

unlock ideadMatching modal form
    - modal trigger : (onload)
    - if user doesn't have get tested enneagram, load test form.

make user home page
    - home page control button
    - user picture
    - user background
    - user nickname
    - user introducing
    - user interest/hobby
    - user userblog
        - blog writing function(simple)