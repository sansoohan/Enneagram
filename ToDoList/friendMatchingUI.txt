set friendMatching icon locked
    - after filled up enneagram from user, change the icon to unlocked one.

unlock friendMatching modal form
    - modal trigger : (onload)
    - if user doesn't have get tested enneagram, load test form.

user check form for random chat
    - default is all checked and each reverse button. It is saved as user data.
    - gender(check box).
    - countury(check box).

get user's countury
    - GPS
    - google firebase analyzing